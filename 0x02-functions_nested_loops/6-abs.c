#include "main.h"

/**
 * _abs - it do be giving positives.
 *
 * @n: passed number.
 *
 * Return: n but positive.
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}

