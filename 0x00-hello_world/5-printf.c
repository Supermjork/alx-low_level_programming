#include<stdio.h>

/**
 * main - Entry point
 *
 * Description: CONTINUATION ONG
 *
 * Return: Always 0 (Success)
 */
int main()
{
	/* BOI DO C98 COMMENTS HIT DIFF */
	char OutStr[] = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", OutStr);
	return (0);
}
